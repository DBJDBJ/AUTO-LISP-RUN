-----------------------------------------------------------------------------
enviroment : EYE
list of functions available
    ?<function> is help text for every <function>
-----------------------------------------------------------------------------
exit            ; exiting the system
test            ; for developers only
Editeye		;
EDITTAR		;
EYE		;
EYEDATA		;
MVM		;
NEWEOLDT	;
NEWEYE		;
TEST		;
USEEYE		;
USETAR		;
